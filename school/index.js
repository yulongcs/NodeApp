var klass = require('./klass');

klass.add('Scott', ['stuA', 'stuB']);

// exports.add = function(klasses) {
// 	klasses.forEach(function(item, index) {
// 		var _klass = item;
// 		var teacherName = item.teacherName;
// 		var studets = item.studets;

// 		klass.add(teacherName, studets);
// 	});
// };